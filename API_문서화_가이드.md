# FastAPI 백엔드 API 문서화 가이드

본 문서는 캡스톤 디자인 최종보고서 작성을 위한 API 코드 스크린샷 가이드입니다.

## 📋 목차

1. [인증 API (Authentication API)](#1-인증-api-authentication-api)
2. [대화 관리 API (Conversations API)](#2-대화-관리-api-conversations-api)
3. [메인 애플리케이션 설정](#3-메인-애플리케이션-설정)

---

## 1. 인증 API (Authentication API)

**파일 위치**: `backend/api/auth.py`

### 1.1 이메일 인증 요청
- **엔드포인트**: `POST /api/auth/send-verification-email`
- **설명**: 회원가입 전 이메일 인증 코드를 발송하는 API
- **주요 기능**:
  - 백석대학교 이메일(@bu.ac.kr) 검증
  - 중복 이메일 확인
  - 6자리 인증 코드 생성 및 이메일 발송
  - 30분 만료 시간 설정
- **스크린샷 포함 범위**: 
  - 라인 28-94 (함수 전체)
  - 특히 라인 42-77 (비즈니스 로직 부분)

### 1.2 이메일 인증 확인
- **엔드포인트**: `POST /api/auth/verify-email`
- **설명**: 이메일로 받은 인증 코드를 확인하는 API
- **주요 기능**:
  - 인증 코드 검증
  - 만료 시간 확인
  - 인증 완료 상태 업데이트
- **스크린샷 포함 범위**: 
  - 라인 97-154 (함수 전체)
  - 특히 라인 112-137 (검증 로직 부분)

### 1.3 회원가입
- **엔드포인트**: `POST /api/auth/signup`
- **설명**: 새로운 사용자를 등록하는 API
- **주요 기능**:
  - 이메일 인증 여부 확인
  - 중복 이메일 확인
  - 비밀번호 해싱 (bcrypt)
  - 사용자 정보 MongoDB 저장
- **스크린샷 포함 범위**: 
  - 라인 157-261 (함수 전체)
  - 특히 라인 176-243 (회원가입 처리 로직)

### 1.4 로그인
- **엔드포인트**: `POST /api/auth/login`
- **설명**: 사용자 인증 후 JWT 토큰을 발급하는 API
- **주요 기능**:
  - 이메일/비밀번호 검증
  - JWT Access Token 및 Refresh Token 발급
  - 사용자 정보 반환
- **스크린샷 포함 범위**: 
  - 라인 264-356 (함수 전체)
  - 특히 라인 314-338 (토큰 생성 및 응답 부분)

### 1.5 비밀번호 재설정 요청
- **엔드포인트**: `POST /api/auth/password-reset/request`
- **설명**: 등록된 이메일로 비밀번호 재설정 인증 코드를 발송하는 API
- **주요 기능**:
  - 등록된 이메일 확인
  - 비밀번호 재설정 코드 생성 및 발송
  - 기존 코드 삭제 후 새 코드 저장
- **스크린샷 포함 범위**: 
  - 라인 359-443 (함수 전체)
  - 특히 라인 392-426 (코드 생성 및 발송 부분)

### 1.6 비밀번호 재설정 확인
- **엔드포인트**: `POST /api/auth/password-reset/confirm`
- **설명**: 인증 코드를 확인하고 새 비밀번호로 변경하는 API
- **주요 기능**:
  - 인증 코드 검증
  - 만료 시간 확인
  - 비밀번호 해싱 및 업데이트
  - 사용된 토큰 삭제
- **스크린샷 포함 범위**: 
  - 라인 446-553 (함수 전체)
  - 특히 라인 465-536 (비밀번호 변경 로직)

### 1.7 Access Token 갱신
- **엔드포인트**: `POST /api/auth/refresh`
- **설명**: Refresh Token을 사용하여 새로운 Access Token을 발급받는 API
- **주요 기능**:
  - Refresh Token 검증
  - 토큰 타입 확인
  - 사용자 존재 여부 확인
  - 새 Access Token 발급
- **스크린샷 포함 범위**: 
  - 라인 556-664 (함수 전체)
  - 특히 라인 570-647 (토큰 갱신 로직)

---

## 2. 대화 관리 API (Conversations API)

**파일 위치**: `backend/api/conversations.py`

### 2.1 새 대화 생성
- **엔드포인트**: `POST /api/conversations`
- **설명**: 새로운 대화방을 생성하는 API
- **주요 기능**:
  - 사용자 인증 확인 (JWT)
  - 대화 제목 설정 (기본값: "새 대화")
  - MongoDB에 대화 정보 저장
- **스크린샷 포함 범위**: 
  - 라인 32-69 (함수 전체)
  - 특히 라인 49-62 (대화 생성 및 응답 부분)

### 2.2 대화 목록 조회
- **엔드포인트**: `GET /api/conversations`
- **설명**: 사용자의 모든 대화를 최신순으로 조회하는 API
- **주요 기능**:
  - 사용자 인증 확인
  - 해당 사용자의 모든 대화 조회
  - 최신순 정렬
- **스크린샷 포함 범위**: 
  - 라인 72-100 (함수 전체)
  - 특히 라인 85-93 (조회 및 응답 부분)

### 2.3 특정 대화 조회
- **엔드포인트**: `GET /api/conversations/{conversation_id}`
- **설명**: 특정 대화의 상세 정보를 조회하는 API
- **주요 기능**:
  - 대화 ID 검증
  - 사용자 권한 확인 (본인 대화만 조회 가능)
  - 대화 상세 정보 반환
- **스크린샷 포함 범위**: 
  - 라인 103-142 (함수 전체)
  - 특히 라인 117-125 (조회 및 응답 부분)

### 2.4 대화 삭제
- **엔드포인트**: `DELETE /api/conversations/{conversation_id}`
- **설명**: 대화 및 모든 메시지를 삭제하는 API
- **주요 기능**:
  - 대화 ID 검증
  - 사용자 권한 확인
  - 대화 및 관련 메시지 삭제
- **스크린샷 포함 범위**: 
  - 라인 145-184 (함수 전체)
  - 특히 라인 159-167 (삭제 처리 부분)

### 2.5 메시지 목록 조회
- **엔드포인트**: `GET /api/conversations/{conversation_id}/messages`
- **설명**: 특정 대화의 모든 메시지를 순서대로 조회하는 API
- **주요 기능**:
  - 대화 ID 검증
  - 사용자 권한 확인
  - 메시지 목록 조회 (순서대로)
- **스크린샷 포함 범위**: 
  - 라인 187-229 (함수 전체)
  - 특히 라인 201-212 (조회 및 응답 부분)

### 2.6 대화방에 메시지 추가 (RESTful)
- **엔드포인트**: `POST /api/conversations/{conversation_id}/messages`
- **설명**: 특정 대화방에 메시지를 추가하고 AI 응답을 받는 API
- **주요 기능**:
  - 사용자 메시지 저장
  - AI 챗봇 응답 생성
  - 벡터 검색을 통한 컨텍스트 제공
  - 응답 메시지 저장
- **스크린샷 포함 범위**: 
  - 라인 232-278 (함수 전체)
  - 특히 라인 249-261 (메시지 처리 및 응답 부분)

### 2.7 채팅하기 (자동 대화방 생성)
- **엔드포인트**: `POST /api/conversations/chat`
- **설명**: conversation_id가 없으면 자동으로 새 대화방을 생성하는 API
- **주요 기능**:
  - conversation_id가 없으면 자동 생성
  - conversation_id가 있으면 해당 대화방에 메시지 추가
  - AI 챗봇 응답 생성
- **스크린샷 포함 범위**: 
  - 라인 281-339 (함수 전체)
  - 특히 라인 299-322 (자동 생성 및 채팅 처리 부분)

---

## 3. 메인 애플리케이션 설정

**파일 위치**: `backend/main.py`

### 3.1 FastAPI 애플리케이션 초기화
- **설명**: FastAPI 애플리케이션의 메인 설정 파일
- **주요 기능**:
  - FastAPI 앱 생성 및 설정
  - CORS 미들웨어 설정
  - 보안 미들웨어 설정 (TrustedHost)
  - 예외 핸들러 설정
  - API 라우터 등록
  - MongoDB 연결 이벤트
- **스크린샷 포함 범위**: 
  - 라인 26-34 (FastAPI 앱 생성)
  - 라인 36-65 (미들웨어 설정)
  - 라인 72-113 (예외 핸들러)
  - 라인 115-118 (라우터 등록)
  - 라인 121-137 (시작 이벤트)

### 3.2 루트 엔드포인트
- **엔드포인트**: `GET /api/`
- **설명**: 애플리케이션 상태 확인 엔드포인트
- **스크린샷 포함 범위**: 
  - 라인 140-147 (함수 전체)

### 3.3 헬스 체크 엔드포인트
- **엔드포인트**: `GET /api/health`
- **설명**: 실제 의존성 연결 상태를 확인하는 엔드포인트
- **주요 기능**:
  - Pinecone 연결 상태 확인
  - HyperCLOVA API 키 확인
  - 전체 시스템 상태 반환
- **스크린샷 포함 범위**: 
  - 라인 149-213 (함수 전체)
  - 특히 라인 159-213 (헬스 체크 로직)

### 3.4 메트릭 엔드포인트
- **엔드포인트**: `GET /api/metrics`
- **설명**: 시스템 메트릭 정보를 반환하는 엔드포인트
- **스크린샷 포함 범위**: 
  - 라인 215-225 (함수 전체)

---

## 📸 스크린샷 촬영 가이드

### 권장 사항

1. **코드 가독성**
   - 코드 에디터의 폰트 크기를 적절히 설정 (12-14pt 권장)
   - 코드 하이라이팅이 잘 보이도록 설정
   - 불필요한 UI 요소는 최소화

2. **스크린샷 범위**
   - 각 API 함수의 전체 코드를 포함
   - 함수 시그니처부터 return 문까지 모두 포함
   - 중요한 비즈니스 로직 부분은 확대하여 별도 스크린샷 추가 가능

3. **보고서 작성 시 포함할 내용**
   - API 엔드포인트 (HTTP 메서드 + 경로)
   - API 기능 설명
   - 주요 파라미터 및 응답 형식
   - 코드 스크린샷
   - 간단한 설명 (코드의 주요 로직)

4. **파일 구조**
   ```
   보고서 구조 예시:
   
   3. 백엔드 API 구현
   3.1 인증 API
      3.1.1 이메일 인증 요청 API
        - 엔드포인트: POST /api/auth/send-verification-email
        - 기능: ...
        - 코드: [스크린샷]
        - 설명: ...
      3.1.2 이메일 인증 확인 API
        ...
   3.2 대화 관리 API
      ...
   ```

---

## 📝 API 요약표

| 카테고리 | API 엔드포인트 | HTTP 메서드 | 인증 필요 | 파일 위치 |
|---------|--------------|------------|----------|----------|
| 인증 | `/api/auth/send-verification-email` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/verify-email` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/signup` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/login` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/password-reset/request` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/password-reset/confirm` | POST | ❌ | `backend/api/auth.py` |
| 인증 | `/api/auth/refresh` | POST | ❌ | `backend/api/auth.py` |
| 대화 | `/api/conversations` | POST | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations` | GET | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations/{id}` | GET | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations/{id}` | DELETE | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations/{id}/messages` | GET | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations/{id}/messages` | POST | ✅ | `backend/api/conversations.py` |
| 대화 | `/api/conversations/chat` | POST | ✅ | `backend/api/conversations.py` |
| 시스템 | `/api/` | GET | ❌ | `backend/main.py` |
| 시스템 | `/api/health` | GET | ❌ | `backend/main.py` |
| 시스템 | `/api/metrics` | GET | ❌ | `backend/main.py` |

---

## 🔍 추가 참고사항

### 기술 스택
- **웹 프레임워크**: FastAPI
- **데이터베이스**: MongoDB Atlas
- **인증**: JWT (JSON Web Token)
- **벡터 DB**: Pinecone
- **AI 모델**: HyperCLOVA

### 주요 라이브러리
- `fastapi`: 웹 프레임워크
- `pymongo`: MongoDB 클라이언트
- `bcrypt`: 비밀번호 해싱
- `python-jose`: JWT 토큰 처리
- `pinecone-client`: Pinecone 벡터 DB 클라이언트

### 코드 스타일
- Python 3.x 기반
- 비동기 프로그래밍 (async/await)
- 타입 힌팅 사용
- 상세한 에러 처리 및 로깅

---

## 📌 보고서 작성 체크리스트

- [ ] 각 API 엔드포인트별 코드 스크린샷 촬영 완료
- [ ] API 엔드포인트 및 HTTP 메서드 명시
- [ ] 각 API의 기능 설명 작성
- [ ] 주요 파라미터 및 응답 형식 설명
- [ ] 코드의 주요 로직 설명
- [ ] 인증 필요 여부 명시
- [ ] 에러 처리 방식 설명 (선택사항)
- [ ] 전체 API 구조도 또는 목록 포함 (선택사항)

---

**작성일**: 2024년
**프로젝트**: 백석대학교 챗봇 시스템
**목적**: 캡스톤 디자인 최종보고서 작성

